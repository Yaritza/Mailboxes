<ul>
<% @favorites.each do |mailbox| %>
<li><%=  Mailbox.find(mailbox.mailbox_id).address %></li>
<li>near: <%= mailbox.address_searched %></li>
<% end %>
</ul>


<!-- <thead>
    <tr>
      <th>Mailbox location</th>
      <th>Search reference address</th>
      <th></th>
    </tr>
  </thead>

<tbody>
      <tr>
        <%# @favorites.each do |mailbox| %>
        <td><%#= Mailbox.find(mailbox.mailbox_id).address %>  miles</td>
        <td>near: <%#= mailbox.address_searched. %></td>

        <%# if user_signed_in? %>
        <td><%#= button_to("Make a Favorite", favorites_path(mailbox_id: mailbox.id, address_searched: @query, user_id: current_user.id)) %></td>
        <%# end %>
      </tr>
  <%# end %>
</tbody>
</table>-->
